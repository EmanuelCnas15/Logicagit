const prompt = require ('prompt-sync')();
let numeros = [];
for (let i = 0; i < 9; i++){
    numeros[i] = Number(prompt("Digite um número para a posção " +1));
    console.log("Vetor completo:" , numeros);
}
for (let i = 0; i < 9; i++){
    numeros[i] = Number(prompt("Digite um número para a posção " +1));
    console.log("Vetor completo:" , numeros);
}